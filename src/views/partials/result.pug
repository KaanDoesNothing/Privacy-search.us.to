article(class="message result" data-title=result.title data-url=result.url)
    div(class="message-body")
        a(href=result.url)
            div(class="label")=result.title
        a(href=result.url)=result.url
        div=result.snippet
        
        div(class="field has-grouped")
            div(class="control")
                a(onclick=`previewImage('${result.url}')` data-url=result.url) Preview
            if result.url.includes("watch?v=")
                div(class="control")
                    a(onclick=`watch('${result.url}')`, data-url=result.url, data-type="video") Watch (This uses a Youtube embed)
                div(class="control")
                    a(onclick=`listen('${result.url}')`, data-url=result.url, data-type="audio") Listen