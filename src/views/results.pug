extends layouts/main

block body
    include partials/search

    div(id="results")
        if Results.rows.length > 0
            each result in Results.rows
                include partials/result

    include scripts/results